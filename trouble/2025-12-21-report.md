* **検出時刻:** 18:05
* **通番:** No.1
* **不具合の概要:** `check_models.py` 実行時に `AttributeError` が発生。 `'Model' object has no attribute 'supported_generation_methods'`。
* **原因の提示:** ユーザーから提供されたスクリプトが想定している `google-genai` SDKのプロパティ名が、実際のSDKバージョン（最新版）では `supported_actions` になっているため。
* **影響 (Impact):** モデル一覧の取得が中断され、利用可能なモデルを確認できない。
* **修正案 (Proposed Fix):** `m.supported_generation_methods` を `m.supported_actions` に変更し、その中に `"generateContent"` が含まれているかを確認するロジックに修正する。

* **検出時刻:** 19:40
* **通番:** No.2
* **不具合の概要:** 戦略検証 (`verify_strategies.py`) において、`growth_quality` 戦略が候補を1件も選定できず、出力ファイルが生成されない。
* **原因の提示:** `DataProvider.load_latest_market_data()` のクエリにおいて、成長性指標となる `profit_growth` および `sales_growth` カラムの取得が漏れているため、`engine.py` のフィルタ条件またはスコアリングで意図しない挙動（全て除外、または0点）となっている可能性が高い。
* **影響 (Impact):** 成長株戦略 (`growth_quality`, `growth_momentum`) が機能しない。
* **修正案 (Proposed Fix):** `src/provider.py` のクエリに `MarketData.profit_growth` および `MarketData.sales_growth` を追加する。また、DBスキーマにこれらのカラムが存在するか確認が必要。
* **ステータス:** **修正完了** (2025-12-21 19:50) - `MarketData` モデルへのカラム追加、DBスキーマ更新ロジック修正、プロバイダのクエリ修正を実施。

