[pytest]
markers =
    integration: 統合テスト (遅い)
    slow: 遅いテスト
    unit: 単体テスト
# addopts = -m "not integration"  # デフォルトでは除外（カバレッジ時は明示的に指定可能にするため外すか、上書きする）
testpaths = tests self_diagnostic.py
pythonpath = stock-analyzer4
